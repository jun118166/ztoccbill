你是一个全栈开发工程师，我需要开发一个「多模板Excel自动导入下单录单系统」，请按以下技能要求实现：

1. 技术栈要求：
   - 使用Next.js (App Router) + React + TypeScript开发
   - 部署在Vercel平台，提供可公开访问的URL
   - 数据库使用Vercel Marketplace支持的数据库（Neon/Supabase/Turso）

2. Excel处理能力：
   - 支持xls/xlsx文件解析，兼容1000条以上数据，无内存溢出
   - 支持多模板适配：自动识别不同列名/列顺序的Excel，自动匹配寄件人姓名、收件人电话等字段
   - 提供手动字段映射功能，用户可手动匹配Excel列与系统字段，并支持模板记忆，下次导入同结构文件时自动复用映射关系

3. 在线预览与编辑：
   - 导入后以表格形式预览数据，表头固定、横向滚动
   - 支持单元格直接编辑，Tab键切换单元格，修改后自动更新
   - 编辑时实时校验，错误单元格标红并显示错误提示

4. 数据校验与错误处理：
   - 必填项校验：寄件人/收件人姓名、电话、地址等字段非空校验
   - 格式校验：电话格式、重量/件数正整数、温度枚举值校验
   - 批量错误提示：一次性列出所有错误（行号、字段、错误原因），用户可修改后重新导入
   - 异常处理：空文件、格式错误、无效Sheet等场景友好提示

5. 提交与数据存储：
   - 支持提交订单数据，提交时显示进度条和Loading状态
   - 提交后数据存入数据库，支持已导入订单列表查询，数据从数据库读取而非本地存储

6. 交互与体验：
   - 导入和提交时显示进度条（百分比+当前条数/总条数）
   - 操作反馈：成功/失败Toast提示，支持防重复提交
   - 支持将预览数据导出为Excel文件，导出内容包含当前修改状态

7. 界面与代码规范：
   - 界面布局合理、交互清晰，符合录单系统的使用场景
   - TypeScript类型定义完整，组件拆分合理，代码结构清晰
